services:
  - type: web
    name: skills-docs
    env: static
    staticPublishPath: .
    buildCommand: echo "pre-built static site — no build needed"
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache, must-revalidate
